#include "dlsch_info.h"
#include <vector>
#include <algorithm>
#include <cmath>
#include <stdexcept>

std::vector<int> segmented(std::vector<int> data, F, C ) {

    data.insert(data.end(), L, -1); // позволяет вставлять элементы по позициям. добавляем L нулей

return data
}
//
// Created by morda on 17.10.2025.
//
